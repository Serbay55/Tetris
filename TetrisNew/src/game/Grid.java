package game;

public class Grid {
    //variablen, die der Größe einer Zelle im Grid definieren, damit das Grid mit dem UI übereinstimmt.
    public static int width = 32, height = 32;

    //Initialisiert das Grid für die Spiel-logik.
    public void createArray(){
        int[][] GridArray = new int[10][20];
    }
}
